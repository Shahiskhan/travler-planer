import React from 'react';
import ViewPoints from '../../pages/ViewPoints';

const ViewPointsManagement = ({ miniAdmin = false }) => {
    // This component wraps the existing ViewPoints page
    return <ViewPoints />;
};

export default ViewPointsManagement;
